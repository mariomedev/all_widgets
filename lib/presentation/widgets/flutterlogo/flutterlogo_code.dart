import 'package:flutter/material.dart';

class FlutterlogoCode extends StatelessWidget {
  const FlutterlogoCode({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: FlutterLogo(
        size: 500,
      ),
    );
  }
}