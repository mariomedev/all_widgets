import 'package:flutter/material.dart';

class PlaceholderCode extends StatelessWidget {
  const PlaceholderCode({super.key});

  @override
  Widget build(BuildContext context) {
    return Placeholder();
  }
}