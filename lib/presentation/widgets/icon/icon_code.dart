import 'package:flutter/material.dart';

class IconCode extends StatelessWidget {
  const IconCode({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Icon(
        Icons.home,
        size: 300,
        color: Colors.amber,
      ),
    );
  }
}
