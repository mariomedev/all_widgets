import 'package:flutter/material.dart';

class OutlinedbuttonCode extends StatelessWidget {
  const OutlinedbuttonCode({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: OutlinedButton(
        onPressed: () {},
        child: Text('Outlinedbutton'),
      ),
    );
  }
}
